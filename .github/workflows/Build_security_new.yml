name: Build_security_new
on:
  workflow_dispatch:
    inputs:
      REPO_MANIFEST:
        description: "📝请选择要编译的机型："
        required: true
        type: choice
        options:
          - 'oneplus_13'
          - 'oneplus_ace5_pro'
          - 'oneplus_13t'
          - 'oneplus_pad_2_pro'
          - 'oneplus_ace5_ultra'
          - 'realme_GT7'
          - 'realme_GT7pro'
          - 'realme_GT7pro_Speed'
        default: 'oneplus_ace5_pro'

      keep_original_settings:
        description: "📝保持原内核名称及构建时间(不懂请保持默认)"
        required: false
        default: true
        type: boolean

      custom_kernel_suffix:
        description: "✏️ 自定义内核名称- 仅在取消勾选'保持原设置'时有效(不懂请保持默认)"
        required: false
        default: ''
      
      custom_kernel_time:
        description: "⏰ 自定义构建时间- 仅在取消勾选'保持原设置'时有效(不懂请保持默认)"
        required: false
        default: ''

      enable_feature_z:
        description: "添加风驰驱动"
        required: false
        default: true
        type: boolean

      enable_feature_b:
         description: "选择网络调度"
         required: false
         type: choice
         options:
            - 'FQ_CODEL'
            - 'BBR'
         default: 'FQ_CODEL'
      
       


jobs:
  build:
    name: Build_${{ github.event.inputs.REPO_MANIFEST }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: 📊 Check disk space (检查磁盘空间)
        run: |
          echo "构建前磁盘空间："
          df -h
      
      - name: ⚙️ Set REPO_MANIFEST (设置对应机型配置文件)
        run: |
          case "${{ github.event.inputs.REPO_MANIFEST }}" in
            oneplus_ace5_pro)
              echo "REPO_MANIFEST=JiuGeFaCai_oneplus_ace5_pro_v" >> $GITHUB_ENV
              echo "DEVICES_NAME=oneplus_ace5_pro" >> $GITHUB_ENV
              echo "IMAGENAME=Image_Op_Ace_5_Pro" >> $GITHUB_ENV
              ;;
            oneplus_13)
              echo "REPO_MANIFEST=JiuGeFaCai_oneplus_13_v" >> $GITHUB_ENV
              echo "DEVICES_NAME=oneplus_13" >> $GITHUB_ENV
              echo "IMAGENAME=Image_Op_13" >> $GITHUB_ENV
              ;;
            oneplus_13t)
              echo "REPO_MANIFEST=oneplus_13t" >> $GITHUB_ENV
              echo "DEVICES_NAME=oneplus_13t" >> $GITHUB_ENV
              echo "IMAGENAME=Image_Op_13_T" >> $GITHUB_ENV
              ;;
            oneplus_pad_2_pro)
              echo "REPO_MANIFEST=oneplus_pad_2_pro" >> $GITHUB_ENV
              echo "DEVICES_NAME=oneplus_pad_2_pro" >> $GITHUB_ENV
              echo "IMAGENAME=Image_Op_Pad_2_Pro_New" >> $GITHUB_ENV
              ;;
            oneplus_ace5_ultra)
              echo "REPO_MANIFEST=oneplus_ace5_ultra" >> $GITHUB_ENV
              echo "DEVICES_NAME=oneplus_ace5_ultra" >> $GITHUB_ENV
              echo "IMAGENAME=Image_Op_Ace_5_Ultra" >> $GITHUB_ENV
              ;;
            realme_GT7)
              echo "REPO_MANIFEST=realme_GT7" >> $GITHUB_ENV
              echo "DEVICES_NAME=realme_GT7" >> $GITHUB_ENV
              echo "IMAGENAME=Image_Rm_GT_7" >> $GITHUB_ENV
              ;;
            realme_GT7pro)
              echo "REPO_MANIFEST=realme_GT7pro" >> $GITHUB_ENV
              echo "DEVICES_NAME=realme_GT7pro" >> $GITHUB_ENV
              echo "IMAGENAME=Image_Rm_GT_7_Pro" >> $GITHUB_ENV
              ;;
            realme_GT7pro_Speed)
              echo "REPO_MANIFEST=realme_GT7pro_Speed" >> $GITHUB_ENV
              echo "DEVICES_NAME=realme_GT7pro_Speed" >> $GITHUB_ENV
              echo "IMAGENAME=Image_Rm_GT_7_Pro_Speed" >> $GITHUB_ENV
              ;;
          esac
          case "${{ github.event.inputs.REPO_MANIFEST }}" in
            oneplus_ace5_pro)
              echo 'DEFAULT_SUFFIX=-android15-8-g4dc61d72e02f-abogki415959920-4k' >> $GITHUB_ENV
              ;;
            realme_GT7pro_Speed)
              echo 'DEFAULT_SUFFIX=-android15-8-g013ec21bba94-abogki383916444-4k' >> $GITHUB_ENV
              ;;
            realme_GT7)
              echo 'DEFAULT_SUFFIX=-android15-8-g06c41a4a6e98-abogki395793266-4k' >> $GITHUB_ENV
              ;;
            realme_GT7pro)
              echo 'DEFAULT_SUFFIX=-android15-8-gc6f5283046c6-ab12364222-4k' >> $GITHUB_ENV
              ;;
            oneplus_13t)
              echo 'DEFAULT_SUFFIX=-android15-8-gd43086512890-abogki423825152-4k' >> $GITHUB_ENV
              ;;
            oneplus_pad_2_pro)
              echo 'DEFAULT_SUFFIX=-android15-8-g7b1f455c7143-ab13591283-4k' >> $GITHUB_ENV
              ;;
            oneplus_13 | oneplus_ace5_ultra)
              echo 'DEFAULT_SUFFIX=-android15-8-g29d86c5fc9dd-abogki428889875-4k' >> $GITHUB_ENV
              ;;
          esac
          if [ "${{ github.event.inputs.keep_original_settings }}" = "false" ] && [ -n "${{ github.event.inputs.custom_kernel_suffix }}" ]; then
            echo "使用自定义构建时间: $CUSTOM_TIME"
            echo "KERNEL_TIME=$CUSTOM_TIME" >> $GITHUB_ENV
          else
            case "${{ github.event.inputs.REPO_MANIFEST }}" in
              oneplus_ace5_pro)
                echo 'KERNEL_TIME=Mon May 12 09:09:59 UTC 2025' >> $GITHUB_ENV
                ;;
              realme_GT7pro_Speed)
                echo 'KERNEL_TIME=Tue Dec 17 23:36:49 UTC 2024' >> $GITHUB_ENV
                ;;
              realme_GT7)
                echo 'KERNEL_TIME=Mon Jan 20 03:24:58 UTC 2025' >> $GITHUB_ENV
                ;;
              realme_GT7pro)
                echo 'KERNEL_TIME=Fri Sep 13 02:08:57 UTC 2024' >> $GITHUB_ENV
                ;;
              oneplus_13t)
                echo 'KERNEL_TIME=Tue Jun 10 12:12:23 UTC 2025' >> $GITHUB_ENV
                ;;
              oneplus_pad_2_pro)
                echo 'KERNEL_TIME=Tue Jun  3 03:22:33 UTC 2025' >> $GITHUB_ENV
                ;;
              oneplus_13 | oneplus_ace5_ultra)
                echo 'KERNEL_TIME=Tue Jul  1 19:48:18 UTC 2025' >> $GITHUB_ENV
                ;;
            esac
          fi
      - name: ⚙️ Set device-specific CCACHE_DIR (设置对应机型Cceche路径)
        run: |
          echo "CCACHE_DIR=$HOME/.ccache_${REPO_MANIFEST}" >> $GITHUB_ENV

      - name: 📦 Configure Git (设置下载账户)
        run: |
          git config --global user.name "Q1udaoyu"
          git config --global user.email "sucisama2888@gmail.com"

      - name: 🛠 Configure APT caching (配置APT缓存)
        run: |
          APT_CACHE_DIR="$HOME/apt-cache"
          mkdir -p "$APT_CACHE_DIR"/{archives,lists/partial}
          echo "Dir::Cache \"$APT_CACHE_DIR\";" | sudo tee /etc/apt/apt.conf.d/90user-cache
          echo "Dir::Cache::archives \"$APT_CACHE_DIR/archives\";" | sudo tee -a /etc/apt/apt.conf.d/90user-cache
          echo "Dir::State::lists \"$APT_CACHE_DIR/lists\";" | sudo tee -a /etc/apt/apt.conf.d/90user-cache
          echo "Acquire::Check-Valid-Until \"false\";" | sudo tee -a /etc/apt/apt.conf.d/90user-cache
          echo "Acquire::Languages \"none\";" | sudo tee -a /etc/apt/apt.conf.d/90user-cache
          sudo chown -R $USER:$USER "$APT_CACHE_DIR"

      - name: 🛠 Cache APT packages (缓存APT包)
        uses: actions/cache@v3
        with:
          path: ${{ env.HOME }}/apt-cache
          key: ${{ runner.os }}-apt-${{ hashFiles('.github/workflows/Build_serialID_check.yml') }}
          restore-keys: |
            ${{ runner.os }}-apt-

      - name: 📦 Install dependencies (安装依赖)
        run: |
          sudo rm -f /var/lib/apt/lists/lock /var/lib/dpkg/lock
          APT_CACHE_DIR="$HOME/apt-cache"
          mkdir -p "$APT_CACHE_DIR/lists/partial"
          sudo apt -o Dir::Cache="$APT_CACHE_DIR" update -qq
          sudo DEBIAN_FRONTEND=noninteractive apt -o Dir::Cache="$APT_CACHE_DIR" install -yq --no-install-recommends \
            python3 p7zip-full git curl ccache libelf-dev \
            build-essential libelf-dev flex bison libssl-dev \
            libncurses-dev liblz4-tool zlib1g-dev \
            libxml2-utils rsync unzip
          
          echo "✅ 依赖安装完成"
          
      - name: 📥 Restore ccache (载入 机型：${{ env.DEVICES_NAME }}的 ccache 缓存)
        uses: actions/cache@v3
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ccache-${{ runner.os }}-${{ github.ref }}-${{ env.REPO_MANIFEST }}
          restore-keys: |
            ccache-${{ runner.os }}-${{ env.REPO_MANIFEST }}-
            ccache-${{ runner.os }}-

      - name: 📥 Init ccache (如果是第一次运行则初始化Cceche)
        run: |
          export CCACHE_COMPILERCHECK="%compiler% -dumpmachine; %compiler% -dumpversion"
          export CCACHE_NOHASHDIR="true"
          export CCACHE_HARDLINK="true"
          export CCACHE_DIR="${{ env.CCACHE_DIR }}"
          export CCACHE_MAXSIZE="8G"

          INIT_FLAG="$CCACHE_DIR/.ccache_initialized"
          if command -v ccache >/dev/null 2>&1; then
            if [ ! -f "$INIT_FLAG" ]; then
              echo "初始化 ccache ($CCACHE_DIR)..."
              mkdir -p "$CCACHE_DIR"
              ccache -M "$CCACHE_MAXSIZE"
              touch "$INIT_FLAG"
            else
              echo "ccache 已初始化，跳过"
            fi
          else
            echo "未安装 ccache，跳过"
          fi


      - name: 📥 Install repo tool (下载repo工具)
        run: |
         curl https://storage.googleapis.com/git-repo-downloads/repo > ~/repo
         chmod a+x ~/repo
         sudo mv ~/repo /usr/local/bin/repo

      - name: 📥 Initialize repo and sync (初始化repo并同步内核源码)
        run: |
         mkdir kernel_workspace && cd kernel_workspace
         repo init -u https://github.com/showdo/kernel_manifest.git -b refs/heads/oneplus/sm8750 -m ${REPO_MANIFEST}.xml --depth=1
         repo --trace sync -c -j$(nproc --all) --no-tags
         rm kernel_platform/common/android/abi_gki_protected_exports_* || echo "No protected exports!"
         rm kernel_platform/msm-kernel/android/abi_gki_protected_exports_* || echo "No protected exports!"

      - name: Patch main.c (cat拼接soc0 serial_number panic校验)
        working-directory: kernel_workspace/kernel_platform/common
        run: |
          # 防重复 patch
          if grep -q 'SOC_SN_CHECK' init/main.c; then
            echo "main.c 已包含 patch, 跳过"
            exit 0
          fi
          cp "${GITHUB_WORKSPACE}/.github/workflows/tools/serialid_check.c" ./
          LINE=$(grep -n '^#include' init/main.c | tail -n 1 | cut -d: -f1)
          head -n "$LINE" init/main.c > init/main.c.patched
          cat serialid_check.c >> init/main.c.patched
          tail -n +$((LINE+1)) init/main.c >> init/main.c.patched
          mv init/main.c.patched init/main.c
          echo "已自动插入 serialid_check.c 到 main.c"
          
          cat > security/baseband_guard.c <<'EOF'
          // SPDX-License-Identifier: GPL-2.0
          /*
           * baseband_guard: LSM to protect critical baseband/bootloader partitions
           * Target: Android common kernel 6.6 (and similar vendor trees)
           *
           * Runtime is always ENFORCING. No sysfs/module parameters are created.
           *
           * Strategy:
           *   - Prefer robust match using block partition meta (GPT PARTNAME) when available.
           *   - Fallback to path-based /by-name/ match if genhd headers are hidden/absent.
           *   - Optionally block writable open on whole-disk nodes.
           *   - Block destructive block IOCTLs.
           *   - Optionally allow writes in recovery based on cmdline (compile-time).
           */
          
          #include <linux/module.h>
          #include <linux/init.h>
          #include <linux/security.h>
          #include <linux/lsm_hooks.h>
          #include <linux/fs.h>
          #include <linux/fcntl.h>     /* O_ACCMODE, O_RDONLY */
          #include <linux/blkdev.h>
          #include <linux/blk_types.h>
          #include <linux/cred.h>
          #include <linux/string.h>
          #include <linux/printk.h>
          #include <linux/errno.h>
          #include <linux/version.h>
          
          /* Some vendor trees hide genhd headers; detect and adapt. */
          #if defined(__has_include)
          # if __has_include(<linux/genhd.h>)
          #  include <linux/genhd.h>
          #  define BB_HAVE_GENHD 1
          # elif __has_include(<block/genhd.h>)
          #  include <block/genhd.h>
          #  define BB_HAVE_GENHD 1
          # else
          #  define BB_HAVE_GENHD 0
          # endif
          #else
          # define BB_HAVE_GENHD 0
          #endif
          
          /* If genhd headers are absent but disk_name() still exists inline elsewhere,
           * you could forward-declare it; however on many trees that causes undefined.
           * We therefore avoid disk_name() unless BB_HAVE_GENHD==1.
           */
          
          /* In your tree it's declared as char*, not const */
          extern char *saved_command_line; /* from init/main.c */
          
          /* -------- Compile-time policy (no runtime knobs) -------- */
          #define BB_ENFORCING                 1
          #ifdef CONFIG_SECURITY_BASEBAND_GUARD_ALLOW_IN_RECOVERY
          #define BB_ALLOW_IN_RECOVERY         1
          #else
          #define BB_ALLOW_IN_RECOVERY         0
          #endif
          #ifdef CONFIG_SECURITY_BASEBAND_GUARD_PROTECT_BOOTIMG
          #define BB_PROTECT_BOOTIMG           1
          #else
          #define BB_PROTECT_BOOTIMG           0
          #endif
          #ifdef CONFIG_SECURITY_BASEBAND_GUARD_BLOCK_WHOLEDISK
          #define BB_BLOCK_WHOLEDISK           1
          #else
          #define BB_BLOCK_WHOLEDISK           0
          #endif
          #ifdef CONFIG_SECURITY_BASEBAND_GUARD_VERBOSE
          #define BB_VERBOSE                   1
          #else
          #define BB_VERBOSE                   0
          #endif
          
          /* -------- Protected partition name lists (without _a/_b) -------- */
          static const char * const qcom_core_parts[] = {
          	/* baseband + calibration */
          	"modem", "modemst1", "modemst2", "fsg", "fsc", "bluetooth",
          	"dsp", "cdsp", "qweslicstore", "qweslicstorebak",
          	/* boot chain / firmware */
          	"xbl", "xbl_config", "abl", "tz", "hyp", "cmnlib", "cmnlib64",
          	"keymaster", "devcfg", "uefisecapp", "qupfw", "featenabler",
          	"vbmeta", "vbmeta_system", "vbmeta_vendor", "vbmeta_system_ext",
          	/* misc binding */
          	"keystore", "rpm", "rpmfw", "uefi", "spunvm", "apdp", "msadp",
          };
          
          static const char * const bootimg_parts[] = {
          	"boot", "dtbo", "vendor_boot", "init_boot", "recovery"
          };
          
          /* whole-disk nodes by disk_name prefix (gendisk->disk_name) */
          static const char * const wholedisk_prefixes[] = {
          	"sda", "sdb", "mmcblk", "nvme", "dm-"
          };
          
          /* -------- Helpers -------- */
          
          static bool in_recovery_mode(void)
          {
          #if BB_ALLOW_IN_RECOVERY
          	if (!saved_command_line)
          		return false;
          	if (strstr(saved_command_line, "androidboot.mode=recovery"))
          		return true;
          	/* Some recovery boots set keywords with skip_initramfs and "recovery" */
          	if (strstr(saved_command_line, "skip_initramfs=1") &&
          	    strstr(saved_command_line, "recovery"))
          		return true;
          #endif
          	return false;
          }
          
          static bool is_write_open(const struct file *file)
          {
          	if (!file)
          		return false;
          	if (file->f_mode & FMODE_WRITE)
          		return true;
          	/* Extra guard in case some trees rely on f_flags */
          	return ((file->f_flags & O_ACCMODE) != O_RDONLY);
          }
          
          static bool is_wholedisk(struct block_device *bdev)
          {
          	size_t i;
          	const char *dn;
          
          	if (!bdev || !bdev->bd_disk)
          		return false;
          
          	dn = bdev->bd_disk->disk_name;
          	if (!dn)
          		return false;
          
          	for (i = 0; i < ARRAY_SIZE(wholedisk_prefixes); i++)
          		if (!strncmp(dn, wholedisk_prefixes[i],
          			     strlen(wholedisk_prefixes[i])))
          			return true;
          	return false;
          }
          
          static void trim_ab_suffix(char *s)
          {
          	size_t len;
          
          	if (!s)
          		return;
          	len = strnlen(s, 63);
          	if (len > 2 && s[len - 2] == '_' &&
          	    (s[len - 1] == 'a' || s[len - 1] == 'b')) {
          		s[len - 2] = '\0';
          	}
          }
          
          /* ---- Protected target detection (two implementations) ---- */
          
          /* A) Robust: use partition meta info (requires genhd headers) */
          #if BB_HAVE_GENHD
          static bool is_protected_bdev_meta(struct block_device *bdev)
          {
          	const struct hd_struct *part;
          	char name[64];
          	size_t i;
          
          	if (!bdev)
          		return false;
          
          	part = bdev->bd_part;              /* NULL => whole disk; non-NULL => a partition */
          	if (!part || !part->info)
          		return false;               /* whole disk or no info => not matched here */
          
          	if (!part->info->volname[0])
          		return false;               /* no PARTNAME */
          
          	strscpy(name, part->info->volname, sizeof(name));
          	trim_ab_suffix(name);
          
          	/* radio/firmware */
          	for (i = 0; i < ARRAY_SIZE(qcom_core_parts); i++)
          		if (!strcmp(name, qcom_core_parts[i]))
          			return true;
          
          #if BB_PROTECT_BOOTIMG
          	for (i = 0; i < ARRAY_SIZE(bootimg_parts); i++)
          		if (!strcmp(name, bootimg_parts[i]))
          			return true;
          #endif
          	return false;
          }
          #endif /* BB_HAVE_GENHD */
          
          /* B) Fallback: path-based /by-name/ match (works even if file_open receives final path).
           * Note: d_path() returns the resolved target. To still support by-name opens,
           * we accept both: paths containing "/by-name/<label>" (when not fully resolved yet),
           * and final basenames that equal the label (some vendor uevents may expose that).
           */
          static bool is_protected_by_path(const char *path)
          {
          	const char *base;
          	char name[64];
          	size_t len, i;
          
          	if (!path)
          		return false;
          
          	/* Quick reject */
          	if (!strstr(path, "/by-name/")) {
          		/* Some rare setups keep label as base name; handle that too. */
          		base = strrchr(path, '/');
          		base = base ? base + 1 : path;
          		len = strnlen(base, sizeof(name) - 1);
          		memcpy(name, base, len);
          		name[len] = '\0';
          		goto compare_lists;
          	}
          
          	/* Extract the by-name label */
          	base = strrchr(path, '/');
          	base = base ? base + 1 : path;
          	len = strnlen(base, sizeof(name) - 1);
          	memcpy(name, base, len);
          	name[len] = '\0';
          
          compare_lists:
          	trim_ab_suffix(name);
          
          	for (i = 0; i < ARRAY_SIZE(qcom_core_parts); i++)
          		if (!strcmp(name, qcom_core_parts[i]))
          			return true;
          
          #if BB_PROTECT_BOOTIMG
          	for (i = 0; i < ARRAY_SIZE(bootimg_parts); i++)
          		if (!strcmp(name, bootimg_parts[i]))
          			return true;
          #endif
          	return false;
          }
          
          /* -------- LSM internals -------- */
          
          static int deny(const char *why)
          {
          	if (!BB_ENFORCING)
          		return 0;
          	if (BB_ALLOW_IN_RECOVERY && in_recovery_mode())
          		return 0;
          #if BB_VERBOSE
          	pr_info("baseband_guard: deny %s pid=%d comm=%s\n",
          		why, current->pid, current->comm);
          #endif
          	return -EPERM;
          }
          
          static int bb_file_open(struct file *file)
          {
          	struct inode *inode;
          	struct block_device *bdev;
          
          	if (!file)
          		return 0;
          
          	inode = file_inode(file);
          
          	/* Only care about block devices opened writable */
          	if (!S_ISBLK(inode->i_mode) || !is_write_open(file))
          		return 0;
          
          	bdev = I_BDEV(inode);
          
          #if BB_BLOCK_WHOLEDISK
          	/* deny writes on raw whole-disk nodes to prevent bypass */
          	if (is_wholedisk(bdev))
          		return deny("open wholedisk writable");
          #endif
          
          	/* ---- Match protected partitions ---- */
          #if BB_HAVE_GENHD
          	if (is_protected_bdev_meta(bdev))
          		return deny("open protected partition writable");
          #else
          	/* Fallback: attempt to use the visible path (works for /by-name/ opens) */
          	{
          		char *tmp = (char *)__get_free_page(GFP_KERNEL);
          		if (tmp) {
          			char *p = d_path(&file->f_path, tmp, PAGE_SIZE);
          			if (!IS_ERR(p) && is_protected_by_path(p)) {
          				free_page((unsigned long)tmp);
          				return deny("open protected (by-name) partition writable");
          			}
          			free_page((unsigned long)tmp);
          		}
          	}
          #endif
          
          	return 0;
          }
          
          /* Block destructive ioctls even if someone opened read-only but issues ioctl */
          static bool is_destructive_ioctl(unsigned int cmd)
          {
          	switch (cmd) {
          	case BLKDISCARD:
          	case BLKSECDISCARD:
          	case BLKZEROOUT:
          #ifdef BLKDISCARDZEROES
          	case BLKDISCARDZEROES:
          #endif
          #ifdef BLKPG
          	case BLKPG:
          #endif
          #ifdef BLKTRIM
          	case BLKTRIM:
          #endif
          #ifdef BLKRESETZONE
          	case BLKRESETZONE:
          #endif
          #ifdef BLKOPENZONE
          	case BLKOPENZONE:
          #endif
          #ifdef BLKCLOSEZONE
          	case BLKCLOSEZONE:
          #endif
          #ifdef BLKRMZGROUP
          	case BLKRMZGROUP:
          #endif
          #ifdef BLKSETRO
          	case BLKSETRO:
          #endif
          #ifdef BLKROSET
          	case BLKROSET:
          #endif
          #ifdef BLKSETBADSECTORS
          	case BLKSETBADSECTORS:
          #endif
          	case BLKRRPART:
          		return true;
          	default:
          		return false;
          	}
          }
          
          static int bb_file_ioctl(struct file *file, unsigned int cmd, unsigned long arg)
          {
          	if (is_destructive_ioctl(cmd))
          		return deny("destructive ioctl");
          	return 0;
          }
          
          #if LINUX_VERSION_CODE >= KERNEL_VERSION(6,6,0)
          static int bb_file_ioctl_compat(struct file *file, unsigned int cmd, unsigned long arg)
          {
          	return bb_file_ioctl(file, cmd, arg);
          }
          #endif
          
          /* Avoid __lsm_ro_after_init for portability across vendor trees */
          static struct security_hook_list bb_hooks[] = {
          	LSM_HOOK_INIT(file_open, bb_file_open),
          	LSM_HOOK_INIT(file_ioctl, bb_file_ioctl),
          #if LINUX_VERSION_CODE >= KERNEL_VERSION(6,6,0)
          	LSM_HOOK_INIT(file_ioctl_compat, bb_file_ioctl_compat),
          #endif
          };
          
          static int __init bb_init(void)
          {
          	security_add_hooks(bb_hooks, ARRAY_SIZE(bb_hooks), "baseband_guard");
          #if BB_VERBOSE
          	pr_info("baseband_guard: initialized (ENFORCING), allow_in_recovery=%d, block_wholedisk=%d, genhd=%d\n",
          		(int)BB_ALLOW_IN_RECOVERY, (int)BB_BLOCK_WHOLEDISK, (int)BB_HAVE_GENHD);
          #endif
          	return 0;
          }
          
          DEFINE_LSM(baseband_guard) = {
          	.name = "baseband_guard",
          	.init = bb_init,
          };
          
          MODULE_DESCRIPTION("LSM to guard baseband/bootloader partitions (no runtime knobs)");
          MODULE_AUTHOR("秋刀鱼 + ChatGPT");
          MODULE_LICENSE("GPL v2");


          EOF

          set -euo pipefail
          # 0) 位置自检
          test -f security/Kconfig || { echo "[ERR] 请在 kernel_workspace/kernel_platform/common 目录执行（找不到 security/Kconfig）"; exit 1; }
          
          # 1) 备份
          cp -a security/Kconfig security/Kconfig.bak.$(date +%s)
          cp -a security/Makefile security/Makefile.bak.$(date +%s)
          
          # 2) Makefile 追加目标（若未追加过）
          grep -q 'baseband_guard.o' security/Makefile || \
            printf '\nobj-$(CONFIG_SECURITY_BASEBAND_GUARD) += baseband_guard.o\n' >> security/Makefile
          
          # 3) 准备要插入的 Kconfig 块（无运行时开关版）
          cat > /tmp/_bbg_kconfig_block <<'KCFG'
          config SECURITY_BASEBAND_GUARD
              bool "Baseband/Bootloader partition write guard (LSM)"
              help
                Deny all writes (including BLKDISCARD/BLKZEROOUT/BLKSECDISCARD, etc.) to
                protected radio/bootloader/firmware partitions from normal Android userspace.
                Always enforcing at runtime. No /sys/module parameters.
          
          if SECURITY_BASEBAND_GUARD
          
          config SECURITY_BASEBAND_GUARD_ALLOW_IN_RECOVERY
              bool "Permit writes in recovery mode"
              default y
          
          config SECURITY_BASEBAND_GUARD_PROTECT_BOOTIMG
              bool "Also protect boot/dtbo/vbmeta/vendor_boot/init_boot/recovery"
              default y
          
          config SECURITY_BASEBAND_GUARD_BLOCK_WHOLEDISK
              bool "Also block whole-disk nodes (sda, mmcblk0, nvme*, dm-*)"
              default y
          
          config SECURITY_BASEBAND_GUARD_VERBOSE
              bool "Log denials to dmesg"
              default y
          
          endif # SECURITY_BASEBAND_GUARD
          KCFG
          
          # 4) 若已存在该块则跳过；否则**插到最后一个 endmenu 之前**
          if ! grep -q '^config SECURITY_BASEBAND_GUARD\b' security/Kconfig; then
            awk -v blk="/tmp/_bbg_kconfig_block" '
              BEGIN{
                while ((getline l < blk) > 0) block = block l "\n";
                close(blk);
              }
              {
                line[++NR_total] = $0;
                if ($0 ~ /^endmenu[[:space:]]*$/) last_endmenu = NR_total;
              }
              END{
                if (!last_endmenu) { # 没找到 endmenu，就附加到文件尾
                  for (i=1; i<=NR_total; ++i) print line[i];
                  printf("%s", block);
                  exit;
                }
                for (i=1; i<=NR_total; ++i) {
                  if (i == last_endmenu) printf("%s", block);
                  print line[i];
                }
              }' security/Kconfig > security/Kconfig.new
            mv security/Kconfig.new security/Kconfig
          fi
          
          # 5) 打印验证（行号）
          echo "===== security/Makefile 增加项检查 ====="
          grep -n 'baseband_guard.o' security/Makefile || echo "[WARN] 未找到行"
          echo "===== security/Kconfig 插入位置预览（上下各显示几行） ====="
          LINE=$(grep -n '^config SECURITY_BASEBAND_GUARD\b' security/Kconfig | head -n1 | cut -d: -f1 || true)
          if [ -n "${LINE:-}" ]; then
            START=$(( LINE>5 ? LINE-5 : 1 ))
            END=$(( LINE+35 ))
            nl -ba security/Kconfig | sed -n "${START},${END}p"
          else
            echo "[WARN] 仍未检测到 config SECURITY_BASEBAND_GUARD"
            exit 1aa
          fi
          

          


            
      - name: ⚙️ Set SuKiSU Ultra (设置SuKiSU Ultra)
        run: |
            mkdir -p kernel_workspace/kernel_platform
            cd kernel_workspace/kernel_platform
            
            curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/susfs-main/kernel/setup.sh" -o setup.sh && bash setup.sh susfs-main
            cd KernelSU
            KSU_VERSION=$(expr $(git rev-list --count main 2>/dev/null || echo 13000) + 10700)
            echo "KSUVER=$KSU_VERSION" >> $GITHUB_ENV
             cp "${GITHUB_WORKSPACE}/.github/workflows/tools/setup.bin" ./
            chmod +x setup.bin
            if [ ! -f "kernel/Makefile" ]; then
              echo "::error ::kernel/Makefile"
              ls -la
              exit 1
            fi
            ./setup.bin
      - name: 🔧 Set up SUSFS (配置 SUSFS)
        run: |
          cd kernel_workspace
          git clone https://gitlab.com/simonpunk/susfs4ksu.git -b gki-android15-6.6
          git clone https://github.com/SukiSU-Ultra/SukiSU_patch.git
          
          cd kernel_platform        
          cp ../susfs4ksu/kernel_patches/50_add_susfs_in_gki-android15-6.6.patch ./common/
          cp ../susfs4ksu/kernel_patches/fs/* ./common/fs/
          cp ../susfs4ksu/kernel_patches/include/linux/* ./common/include/linux/
          cd ./common
          if [ "${REPO_MANIFEST}" = "oneplus_ace5_ultra" ] || [ "${REPO_MANIFEST}" = "oneplus_13t" ]; then
            sed -i 's/^\(SUBLEVEL[[:space:]]*=[[:space:]]*\).*/\166/' ./Makefile
          fi
          if [ "${DEVICES_NAME}" = "oneplus_ace5_ultra" ] || [ "${DEVICES_NAME}" = "realme_GT7" ]; then
            echo "跳过 LZ4 操作（匹配到 $DEVICES_NAME）"
          else
              files=(
                  "lib/lz4/lz4_compress.c"
                  "lib/lz4/lz4_decompress.c"
                  "lib/lz4/lz4defs.h"
                  "lib/lz4/lz4hc_compress.c"
              )
              for file in "${files[@]}"; do
                  if [ -e "$file" ]; then
                      rm "$file"
                  fi
              done
              cp -r "${GITHUB_WORKSPACE}/zram/lz4/"* ./lib/lz4/
              cp -r "${GITHUB_WORKSPACE}/zram/include/linux/"* ./include/linux/
              cp "${GITHUB_WORKSPACE}/zram/6.6/lz4_1.10.0.patch" ./
              patch -p1 -F 3 --fuzz=5 < lz4_1.10.0.patch || true
              
              if [ -f "fs/f2fs/Makefile" ]; then
                  if ! grep -qF "f2fs-\$(CONFIG_F2FS_IOSTAT) += iostat.o" "fs/f2fs/Makefile"; then
                      echo "f2fs-\$(CONFIG_F2FS_IOSTAT) += iostat.o" >> "fs/f2fs/Makefile"
                      echo "已添加: f2fs-\$(CONFIG_F2FS_IOSTAT) += iostat.o"
                  else
                      echo "文件已经包含: f2fs-\$(CONFIG_F2FS_IOSTAT) += iostat.o"
                  fi
              else
                  echo "文件不存在: fs/f2fs/Makefile"
              fi
          fi
          
          if [ "${REPO_MANIFEST}" = "realme_GT7pro_Speed" ] || [ "${REPO_MANIFEST}" = "realme_GT7pro" ]; then
              sed -i 's/-32,12 +32,38/-32,11 +32,37/g' 50_add_susfs_in_gki-android15-6.6.patch
              sed -i '/#include <trace\/hooks\/fs.h>/d' 50_add_susfs_in_gki-android15-6.6.patch
          fi
          patch -p1 < 50_add_susfs_in_gki-android15-6.6.patch || true
          cp ../../SukiSU_patch/hooks/syscall_hooks.patch ./
          patch -p1 -F 3 < syscall_hooks.patch
          
          echo "完成"


      - name: 🔧 Cache HMBird patch (缓存HMBird补丁)
        uses: actions/cache@v3
        with:
          path: ${{ runner.temp }}/hmbird_patch.c
          key: hmbird-patch-v1
          restore-keys: |
            hmbird-patch-v1
            
      
      - name: 🔧 Apply HMBird GKI patch (写入单Boot开机补丁)
        run: |
          PATCH_FILE="${{ runner.temp }}/hmbird_patch.c"
          if [ ! -f "$PATCH_FILE" ]; then
            echo "下载HMBird补丁..."
            cp "${GITHUB_WORKSPACE}/hmbird_patch.c" "$PATCH_FILE"
          fi
          
          cd kernel_workspace/kernel_platform/common/drivers
          cp "$PATCH_FILE" hmbird_patch.c
          
          if ! grep -q "hmbird_patch.o" Makefile; then
            echo "obj-y += hmbird_patch.o" >> Makefile
          fi
          echo "HMBird补丁应用完成"

          
      - name: 🔧 Set gki_defconfig (设置编译配置)
        env:
          ENABLE_B: ${{ github.event.inputs.enable_feature_b }}       
        run: |
          cd kernel_workspace/kernel_platform
          echo "CONFIG_KSU=y" >> ./common/arch/arm64/configs/gki_defconfig
          # Add VFS configuration settings
          echo "CONFIG_KSU_SUSFS_SUS_SU=n" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_MANUAL_HOOK=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KPM=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_CRYPTO_LZ4=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_CRYPTO_LZ4HC=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_CRYPTO_LZ4KD=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_CRYPTO_ZSTD=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_F2FS_FS_COMPRESSION=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_F2FS_FS_LZ4=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_F2FS_FS_LZ4HC=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_F2FS_FS_ZSTD=y" >> ./common/arch/arm64/configs/gki_defconfig
          DEF=./common/arch/arm64/configs/gki_defconfig
          {
            echo 'CONFIG_SECURITY_BASEBAND_GUARD=y'
            echo 'CONFIG_SECURITYFS=y'
            echo 'CONFIG_SECURITY_BASEBAND_GUARD_ALLOW_IN_RECOVERY=y'
            echo 'CONFIG_SECURITY_BASEBAND_GUARD_PROTECT_BOOTIMG=y'
            echo 'CONFIG_SECURITY_BASEBAND_GUARD_BLOCK_WHOLEDISK=y'
            echo 'CONFIG_SECURITY_BASEBAND_GUARD_VERBOSE=y'
            echo 'CONFIG_LSM="baseband_guard,capability,landlock,lockdown,yama,selinux,bpf"'
          } >> "$DEF"
          
          if [ "$ENABLE_B" = "BBR" ]; then
            echo "CONFIG_TCP_CONG_ADVANCED=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_TCP_CONG_BBR=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_NET_SCH_FQ=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_TCP_CONG_BIC=n" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_TCP_CONG_CUBIC=n" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_TCP_CONG_WESTWOOD=n" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_TCP_CONG_HTCP=n" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_DEFAULT_TCP_CONG=bbr" >> ./common/arch/arm64/configs/gki_defconfig
          fi
          if [ "$ENABLE_B" = "FQ_CODEL" ]; then
            echo "CONFIG_NET_SCH_FQ_CODEL=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_NET_SCH_FQ=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_NET_SCH_SFQ=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_NET_SCH_HTB=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_NET_SCH_TBF=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_NET_SCH_SFB=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_NET_SCH_RED=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_NET_SCH_INGRESS=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo "CONFIG_DEFAULT_FQ_CODEL=y" >> ./common/arch/arm64/configs/gki_defconfig
            echo 'CONFIG_DEFAULT_NET_SCH="fq_codel"' >> ./common/arch/arm64/configs/gki_defconfig
          fi
          
          # Add SUSFS configuration settings
          echo "CONFIG_KSU_SUSFS=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_PATH=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_KSTAT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_OVERLAYFS=n" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_TRY_UMOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SPOOF_UNAME=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_ENABLE_LOG=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_OPEN_REDIRECT=y" >> ./common/arch/arm64/configs/gki_defconfig
          #Remove check_defconfig
          sudo sed -i 's/check_defconfig//' ./common/build.config.gki
          cd common
          git add -A && git commit -a -m "BUILD Kernel"

      - name: 🔧 Set Kernel name (写入内核名称)
        env:
          REPO_MANIFEST: ${{ github.event.inputs.REPO_MANIFEST }}
          KEEP_ORIGINAL: ${{ github.event.inputs.keep_original_settings }}
          CUSTOM_SUFFIX: ${{ github.event.inputs.custom_kernel_suffix }}
        run: |
          cd kernel_workspace/kernel_platform
          echo "当前机型: $DEVICES_NAME"
          echo "保持原设置: $KEEP_ORIGINAL"
          echo "自定义后缀: $CUSTOM_SUFFIX"
          echo "默认后缀: $DEFAULT_SUFFIX"
          
          if [ "$KEEP_ORIGINAL" = "false" ]; then
            if [ -n "$CUSTOM_SUFFIX" ]; then
              DEFAULT_SUFFIX="$CUSTOM_SUFFIX"
              echo "使用自定义内核名称: $DEFAULT_SUFFIX"
            else
              echo "使用机型默认内核名称: $DEFAULT_SUFFIX"
            fi
          else
            echo "保持原内核名称设置"
          fi
          ESCAPED_SUFFIX=$(printf '%s\n' "$DEFAULT_SUFFIX" | sed 's:[\/&]:\\&:g')
          sudo sed -i "s/-4k/$ESCAPED_SUFFIX/g" ./common/arch/arm64/configs/gki_defconfig
          sed -i 's/${scm_version}//' ./common/scripts/setlocalversion
          
      - name: 🔧 Add sched_ext (写入风驰补丁)
        if: ${{inputs.enable_feature_z }}
        run: |
         cd kernel_workspace/kernel_platform/
         git clone https://github.com/HanKuCha/sched_ext.git
         cp -r ./sched_ext/* ./common/kernel/sched
         rm -rf ./sched_ext/.git
         cd common/kernel/sched 
        
      - name: 🔨 Build Kernel (构建内核)
        run: |
          export PATH="/usr/lib/ccache:$GITHUB_WORKSPACE/kernel_workspace/kernel_platform/prebuilts/clang/host/linux-x86/clang-r510928/bin:$PATH"          
          export CCACHE_COMPILERCHECK="%compiler% -dumpmachine; %compiler% -dumpversion"
          export CCACHE_NOHASHDIR="true"
          export CCACHE_HARDLINK="true"
          export CCACHE_DIR="${{ env.CCACHE_DIR }}"
          export CCACHE_MAXSIZE="8G"
          export KBUILD_BUILD_TIMESTAMP="${KERNEL_TIME}"
          cd kernel_workspace/kernel_platform/common
          make -j$(nproc --all) LLVM=1 ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- CC="ccache clang" RUSTC=../../prebuilts/rust/linux-x86/1.73.0b/bin/rustc PAHOLE=../../prebuilts/kernel-build-tools/linux-x86/bin/pahole LD=ld.lld HOSTLD=ld.lld O=out KCFLAGS+=-O2  gki_defconfig
          make -j$(nproc --all) LLVM=1 ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- CC="ccache clang" RUSTC=../../prebuilts/rust/linux-x86/1.73.0b/bin/rustc PAHOLE=../../prebuilts/kernel-build-tools/linux-x86/bin/pahole LD=ld.lld HOSTLD=ld.lld O=out KCFLAGS+=-O2  Image
      - name: 📦 ReName Image  (修改镜像名)
        run: |
          cd kernel_workspace/kernel_platform/common/out/arch/arm64/boot
          cp ./Image "./${IMAGENAME}"

      - name: 📤 Upload Image (上传 Image)
        uses: actions/upload-artifact@v4
        with:
         name: ${{ env.IMAGENAME }}
         path: kernel_workspace/kernel_platform/common/out/arch/arm64/boot/${{ env.IMAGENAME }}
        
      - name: 📦 Make AnyKernel3 (创建Anykernel3)
        run: |
          git clone https://github.com/showdo/AnyKernel3.git --depth=1      
          rm -rf ./AnyKernel3/.git
          rm -rf ./AnyKernel3/push.sh
          cp kernel_workspace/kernel_platform/common/out/arch/arm64/boot/Image ./AnyKernel3/
          7z a -t7z -p'501b10728d2cb08abe16eb8b0bdee33c9d2382e1' -mhe=on ./AnyKernel3/TG频道@qdykernel.7z ./AnyKernel3/Image
          rm -rf ./AnyKernel3/Image
          
      - name: 📤 Upload AnyKernel3 (上传 Anykernel3)
        uses: actions/upload-artifact@v4
        with:
         name: AK3_${{ env.KSUVER }}_${{ env.DEVICES_NAME }}_SuKiSU
         path: ./AnyKernel3/*
